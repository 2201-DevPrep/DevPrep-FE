import React, { useState } from 'react';
import './DeckDropdown.scss'

const DeckDropdown = () => {
    const dropdownRef = useRef(null)
    const [isActive, setIsActive] = useState(false);
    return (
        <>
        </>
    );
}

export default DeckDropdown;